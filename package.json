{
  "scripts": {
    "build": "tsc && yarn build:cfg",
    "test": "mocha -r ts-node/register src/**/*.spec.ts"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@types/mocha": "^8.2.0",
    "chai": "^4.2.0",
    "mocha": "^8.2.1",
    "ts-node": "^9.1.1",
    "ts-sinon": "^2.0.1",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "moment": "^2.29.1"
  }
}